[package]
name = "converter"
version = "1.0.2"
edition = "2024"
description = "Report files converter"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
parser = { path = "../parser" }