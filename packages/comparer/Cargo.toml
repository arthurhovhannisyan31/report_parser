[package]
name = "comparer"
version = "1.0.1"
edition = "2024"
description = "Report files comparer"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
parser = { path = "../parser" }